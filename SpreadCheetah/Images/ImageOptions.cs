namespace SpreadCheetah.Images;

public sealed class ImageOptions
{
    public ImageSize? Size { get; set; }

    // TODO: Future feature: Specifying the image name (must be unique across all sheets)
    // TODO: Offsets. Future version?
}
